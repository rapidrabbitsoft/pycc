[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pycc = {file = ".", editable = true}

[dev-packages]
black = ">=22.0.0"
isort = ">=5.0.0"
flake8 = ">=4.0.0"
pylint = ">=2.12.0"
mypy = ">=0.950"
bandit = ">=1.7.0"
safety = ">=1.10.0"
pydocstyle = ">=6.0.0"
vulture = ">=2.0.0"
radon = ">=5.0.0"
pytest = ">=7.0.0"
pytest-cov = ">=3.0.0"
coverage = ">=6.0.0"
pre-commit = ">=2.20.0"

[requires]
python_version = "3.10"
